SHOW DATABASES;
USE albums_db;
SHOW CREATE DATABASE albums_db;
SHOW TABLES;
USE employees;
SHOW TABLES;
SELECT * FROM dept_manager;
-- Which table(s) do you think contain a numeric type column?
-- Employee Number is the only numerical type column

-- Which table(s) do you think contain a string type column?
-- First Name, Last Name

-- Which table(s) do you think contain a date type column?
-- Birth Date, Hire Date

-- What is the relationship between the employees and the departments tables? 
/*In the dept_emp tab, we can see the employee number and hire date, 
so we can see more information on the same employee